---

import {getAllTags} from '../../src/lib/getTags.js';

const tags = await getAllTags();

---
<div class="section-container">

<h1>Orbits🌀</h1>
</div>
  <ul class="tag_grid">
  {tags.map(tag => (
  <li><a href={`${tag}`} class="tag"><h5>#{tag}</h5></a></li>
  ))}
  </ul>

<style>

  .section-container{
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    margin-top: 2rem;
  };
  h1{
    text-align: center;
  }
  ul{
    
    width:100%;
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    list-style: none;
    padding: 0;
    margin-top: 8px;
    justify-content: center;
    align-items: center;
  }

  ul li {
    width: 50%;
  }
.tag-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    margin: 1rem 0;
  }

.tag {
     width: 100%;
    display: block;
    text-align: center;
    padding: 0.5rem 1rem;
    background-color: var(--accent);
    color: var(--dark, #414245);
    text-decoration: none;
    transition: background 0.2s, color 0.2s;
    cursor: pointer;
  }

  .tag:hover {
    background-color: var(--gray-light );
    color: var(--black, #414245);
  }



  
</style>
